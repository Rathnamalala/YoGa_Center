# Enable URL rewriting
RewriteEngine On

# Redirect requests without file extensions to .html
RewriteCond %{REQUEST_FILENAME} !-d
RewriteCond %{REQUEST_FILENAME} !-f
RewriteRule ^(.*)$ $1.html [L,QSA]
